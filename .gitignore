# Credentials — NEVER commit these
.env
*.pem
checkpoints/access_token.json

# Checkpoints — runtime state, never commit (EC2 pulls from S3; local reads from disk)
checkpoints/*.json
checkpoints/short_wr_opt/*.json

# Data files (too large for git — re-download on AWS via data pipeline)
data/stocks/
data/raw/
data/index/
data/trade_logs/

# Memory / strategy agent (runtime-generated, EC2 writes its own)
memory/strategy_agent.md

# Claude Code project settings (personal/local)
.claude/

# Runtime logs (regenerated automatically)
logs/

# Jupyter checkpoints
.ipynb_checkpoints/
**/.ipynb_checkpoints/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
*.egg-info/
dist/
build/
.eggs/

# Tests
.pytest_cache/

# OS
.DS_Store
Thumbs.db
